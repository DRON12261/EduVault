@model EduVault.Pages.FileTypes.FileTypeModel

@if (Model.Fields != null && Model.Fields.Any())
{
    <!--<form id="saveFieldsForm" asp-antiforgery="true">
        @Html.AntiForgeryToken()
        <button asp-page-handler="SaveFields" type="submit"  class="btn btn-primary">Сохранить поля</button>
        <table class="table">
            <thead>
                <tr>
                    <th>Название</th>
                    <th>Обязательное</th>
                    <th>Автозаполняемое</th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                @for (var i = 0; i < Model.Fields.Count; i++)
                {
                    <tr>
                        <td>
                            <input asp-for="Fields[i].Name" class="form-control" />
                            <input type="hidden" asp-for="Fields[i].Id" />
                            <input type="hidden" asp-for="Fields[i].FileTypeId" />
                        </td>
                        <td>
                            <input type="checkbox" asp-for="Fields[i].IsRequired" class="form-check-input" />
                        </td>
                        <td>
                            <input type="checkbox" asp-for="Fields[i].IsPrefilled" class="form-check-input" />
                        </td>
                        <td>
                            <button type="button" class="btn btn-danger remove-field" data-index="@i">
                                Удалить
                            </button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </form>-->
    <table class="table">
        <thead>
            <tr>
                <th>Название</th>
                <th>Обязательное</th>
                <th>Автозаполняемое</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            @for (var i = 0; i < Model.Fields.Count; i++)
            {
                <tr>
                    <td>
                        <input asp-for="Fields[i].Name" class="form-control" />
                        <input type="hidden" asp-for="Fields[i].Id" />
                        <input type="hidden" asp-for="Fields[i].FileTypeId" />
                    </td>
                    <td>
                        <input type="checkbox" asp-for="Fields[i].IsRequired" class="form-check-input" />
                    </td>
                    <td>
                        <input type="checkbox" asp-for="Fields[i].IsPrefilled" class="form-check-input" />
                    </td>
                    <td>
                        <button type="button" class="btn btn-danger remove-field" data-index="@i">
                            Удалить
                        </button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="alert alert-info">Нет добавленных полей</div>
}
